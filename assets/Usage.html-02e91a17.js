import{_ as e,p as a,q as n,a1 as o}from"./framework-69d00ee0.js";const i={},s=o(`<h1 id="usage" tabindex="-1"><a class="header-anchor" href="#usage" aria-hidden="true">#</a> Usage</h1><h2 id="informations" tabindex="-1"><a class="header-anchor" href="#informations" aria-hidden="true">#</a> Informations</h2><p>The command line interface has several prefixes which are <code>sheweny</code> and <code>shw</code>, so for the following commands, you can use one of the 2 prefixes. The help command is also at your disposal if you are too lazy to come to the guide.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sheweny <span class="token parameter variable">--help</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h2 id="create-command" tabindex="-1"><a class="header-anchor" href="#create-command" aria-hidden="true">#</a> <code>Create</code> command</h2><p>To create a project, you just need to run the following command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sheweny create <span class="token punctuation">[</span>project_name<span class="token punctuation">]</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then you will have a series of questions that will allow you to refine the result of your project creation and to have a bot start that meets your expectations. Depending on what you have answered, you will have access from the start, the creation of the project has commands, events, or interactions.</p><p>For more information, you can run the help command: <code>sheweny --help create</code></p><h2 id="add-command" tabindex="-1"><a class="header-anchor" href="#add-command" aria-hidden="true">#</a> <code>Add</code> command</h2><p>To add a template in your project with the sheweny framework, you just need to run the following command.</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>sheweny <span class="token function">add</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You have several types of template addition depending on what you want to add</p><ul><li>command</li><li>event</li><li>button</li><li>selectmenu</li><li>inhibitor</li></ul><p>After choosing the type of addition, you can answer the questions that are offered to once again refine the result of adding a template.</p><p>For more information, you can run the help command: <code>sheweny --help add</code>.</p><h2 id="init-command" tabindex="-1"><a class="header-anchor" href="#init-command" aria-hidden="true">#</a> <code>Init</code> command</h2><p>This command is used to initialize the Command Line Interface (CLI) with <strong>an existing project</strong> .</p><h2 id="version-command" tabindex="-1"><a class="header-anchor" href="#version-command" aria-hidden="true">#</a> <code>Version</code> command</h2><p>This command return the version of the CLI (not the framework).</p><hr><p>After having done all this, you can of course edit the project code as you wish with an editor, using the rest of the Guide or with the Documentation.</p><h2 id="common-errors" tabindex="-1"><a class="header-anchor" href="#common-errors" aria-hidden="true">#</a> Common errors</h2><h3 id="execution-policy" tabindex="-1"><a class="header-anchor" href="#execution-policy" aria-hidden="true">#</a> Execution Policy</h3><p>If you have an error :</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>File cannot be loaded because the execution of scripts is disabled on this system. Please see <span class="token string">&quot;get-help about_signing&quot;</span> <span class="token keyword">for</span> <span class="token function">more</span> details
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Changed script execution parameter. On an administrator powershell, you can do this by running the following command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>Set-ExecutionPolicy Unrestricted
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,28),t=[s];function d(r,c){return a(),n("div",null,t)}const l=e(i,[["render",d],["__file","Usage.html.vue"]]);export{l as default};
